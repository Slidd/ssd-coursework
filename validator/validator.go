package validator

func ChangeTicketType() {
	// Check if user has the correct role to change the type of ticket
	// Pass in before value and after value
	// Get user's role from session
	// If value is the same
	// Return true
	// if value has changed and user has correct role
	// Return true
	// If value has changed and user does not have correct role
	// return false
	// return false
}
